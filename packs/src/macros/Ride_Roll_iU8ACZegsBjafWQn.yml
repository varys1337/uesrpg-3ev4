_id: iU8ACZegsBjafWQn
name: Ride Roll
type: script
author: KoTybT0dunV5wS6T
img: icons/environment/creatures/horse-brown.webp
scope: global
command: |-
  let d = new Dialog({
      title: "Apply Roll Modifier",
      content: `<form>
                  <div class="dialogForm">
                  <label><b>Ride Modifier: </b></label><input placeholder="ex. -20, +10" id="playerInput" value="0" style=" text-align: center; width: 50%; border-style: groove; float: right;" type="text"></input></div>
                </form>`,
      buttons: {
        one: {
          label: "Roll!",
          callback: html => {
            const playerInput = parseInt(html.find('[id="playerInput"]').val());
            let roll = new Roll("1d100");
            roll.roll({async:false});
          
            // Defensive access to avoid TypeError when .system or nested fields are missing
            const actorSys = token.actor?.system;
            const lucky = actorSys?.lucky_numbers || {};
            const unlucky = actorSys?.unlucky_numbers || {};
            const rideItem = token.actor?.items?.find(entry => entry.name === "Ride");
            const rideVal = rideItem?.system?.value ?? 0;
            const wounded = actorSys?.wounded === true;
          
            if (roll.total === lucky.ln1 || roll.total === lucky.ln2 || roll.total === lucky.ln3 || roll.total === lucky.ln4 || roll.total === lucky.ln5) {
              const content = `Rolls for <b>Ride</b>!
              <p></p><b>Target Number: [[${rideVal}]]</b> <p></p>
              <b>Result: [[${roll.total}]]</b><p></p>
              <span style='color:green; font-size:120%;'> <b>LUCKY NUMBER!</b></span>`
          
              roll.toMessage({ user: game.user._id, speaker: ChatMessage.getSpeaker(), content: content});
          
            } else if (roll.total === unlucky.ul1 || roll.total === unlucky.ul2 || roll.total === unlucky.ul3 || roll.total === unlucky.ul4 || roll.total === unlucky.ul5) {
              const content = `Rolls for <b>Ride</b>!
              <p></p><b>Target Number: [[${rideVal}]]</b> <p></p>
              <b>Result: [[${roll.total}]]</b><p></p>
              <span style='color:red; font-size:120%;'> <b>UNLUCKY NUMBER!</b></span>`
          
              roll.toMessage({ user: game.user._id, speaker: ChatMessage.getSpeaker(), content: content});
          
            } else if (wounded) {
              const content = `Rolls for <b>Ride</b>!
              <p></p><b>Target Number: [[${rideVal}]] </b> <p></p>
              <b>Result: [[${roll.total}]]</b><p></p>
              ${roll.total<=(rideVal + playerInput) ? " <span style='color:green; font-size: 120%;'> <b>SUCCESS!</b></span>" : " <span style='color:red; font-size: 120%;'> <b>FAILURE!</b></span>"}`
          
              roll.toMessage({ user: game.user._id, speaker: ChatMessage.getSpeaker(), content: content});
    
            } else {
              const content = `Rolls for <b>Ride</b>!
              <p></p><b>Target Number: [[${rideVal}]]</b> <p></p>
              <b>Result: [[${roll.total}]]</b><p></p>
              ${roll.total<=(rideVal + playerInput) ? " <span style='color:green; font-size: 120%;'> <b>SUCCESS!</b></span>" : " <span style='color:red; font-size: 120%;'> <b>FAILURE!</b></span>"}`
          
              roll.toMessage({ user: game.user._id, speaker: ChatMessage.getSpeaker(), content: content});
              }
          }
        },
        two: {
          label: "Cancel",
          callback: html => console.log("Cancelled")
        }
      },
      default: "one",
      close: html => console.log()
    });
    d.render(true);
folder: null
sort: 0
flags:
  core:
    sourceId: Macro.2PkHZTeZGb2BDnNf
_stats:
  coreVersion: '12.331'
  systemId: null
  systemVersion: null
  createdTime: null
  modifiedTime: null
  lastModifiedBy: null
  compendiumSource: null
  duplicateSource: null
ownership:
  default: 0
  inSimu0FutxmlwqN: 3
  KoTybT0dunV5wS6T: 3
_key: '!macros!iU8ACZegsBjafWQn'

