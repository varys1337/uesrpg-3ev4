# Styles

Canonical stylesheet sources live in `styles/scss/**`.
Generated runtime stylesheet: `styles/uesrpg.css`.

Use standalone Dart Sass to build `styles/uesrpg.css` from `styles/scss/uesrpg.scss`.
See `docs/dev/styles.md` for exact build and verification commands.
