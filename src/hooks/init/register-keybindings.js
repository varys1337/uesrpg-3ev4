/**
 * Placeholder registry for keybinding registration.
 * Kept as a dedicated module so init orchestration stays stable as keybindings are added.
 */
export function registerKeybindings() {
  // No system keybindings are currently registered.
}
