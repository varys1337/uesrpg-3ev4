/**
 * Reserved change-event handler entry point for chat card controls.
 * Intentionally no-op until chat cards require delegated change handling.
 */
export function registerDelegatedChatChangeHandler() {
  // No delegated change handlers are currently registered.
}
