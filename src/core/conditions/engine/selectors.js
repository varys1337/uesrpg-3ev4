/**
 * src/core/conditions/engine/selectors.js
 *
 * Selector resolution boundary for condition engine.
 *
 * Stage 06 introduces this file as a stable location for effect selection and
 * lookup utilities. Migration is incremental to avoid regressions.
 */

// Intentionally empty for Stage 06. (No behavioral change.)
