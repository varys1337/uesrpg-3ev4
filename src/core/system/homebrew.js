/**
 * Compatibility shim.
 *
 * Canonical homebrew settings helpers now live in src/core/homebrew/settings.js.
 * Keep re-exports here to preserve existing import paths.
 */
export * from "../homebrew/settings.js";
