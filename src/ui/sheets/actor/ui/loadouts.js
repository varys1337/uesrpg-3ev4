/**
 * Loadout management helpers for actor sheets.
 * 
 * Foundry VTT v13 / AppV1-compatible. No schema changes.
 */

/**
 * Save loadout handler.
 * 
 * @param {SimpleActorSheet} sheet - The actor sheet instance
 * @param {Event} event - The save event
 * @returns {Promise<void>}
 */
export async function onLoadoutSave(sheet, event) {
  // Placeholder - will be populated in Phase 4
}

/**
 * Apply loadout handler.
 * 
 * @param {SimpleActorSheet} sheet - The actor sheet instance
 * @param {Event} event - The apply event
 * @returns {Promise<void>}
 */
export async function onLoadoutApply(sheet, event) {
  // Placeholder - will be populated in Phase 4
}

/**
 * Delete loadout handler.
 * 
 * @param {SimpleActorSheet} sheet - The actor sheet instance
 * @param {Event} event - The delete event
 * @returns {Promise<void>}
 */
export async function onLoadoutDelete(sheet, event) {
  // Placeholder - will be populated in Phase 4
}
