/**
 * Menu dialog helpers for actor sheets (XP, race, birthsign, etc.).
 * 
 * Foundry VTT v13 / AppV1-compatible. No schema changes.
 */

/**
 * XP menu handler.
 * 
 * @param {SimpleActorSheet} sheet - The actor sheet instance
 * @param {Event} event - The click event
 * @returns {Promise<void>}
 */
export async function onXPMenu(sheet, event) {
  // Placeholder - will be populated in Phase 4
}

/**
 * Race menu handler.
 * 
 * @param {SimpleActorSheet} sheet - The actor sheet instance
 * @param {Event} event - The click event
 * @returns {Promise<void>}
 */
export async function onRaceMenu(sheet, event) {
  // Placeholder - will be populated in Phase 4
}

/**
 * Birthsign menu handler.
 * 
 * @param {SimpleActorSheet} sheet - The actor sheet instance
 * @param {Event} event - The click event
 * @returns {Promise<void>}
 */
export async function onBirthSignMenu(sheet, event) {
  // Placeholder - will be populated in Phase 4
}

/**
 * Lucky menu handler.
 * 
 * @param {SimpleActorSheet} sheet - The actor sheet instance
 * @param {Event} event - The click event
 * @returns {Promise<void>}
 */
export async function onLuckyMenu(sheet, event) {
  // Placeholder - will be populated in Phase 4
}

/**
 * Set base characteristics handler.
 * 
 * @param {SimpleActorSheet} sheet - The actor sheet instance
 * @param {Event} event - The click event
 * @returns {Promise<void>}
 */
export async function onSetBaseCharacteristics(sheet, event) {
  // Placeholder - will be populated in Phase 4
}

/**
 * Wealth calculator handler.
 * 
 * @param {SimpleActorSheet} sheet - The actor sheet instance
 * @param {Event} event - The click event
 * @returns {Promise<void>}
 */
export async function onWealthCalc(sheet, event) {
  // Placeholder - will be populated in Phase 4
}

/**
 * Carry bonus handler.
 * 
 * @param {SimpleActorSheet} sheet - The actor sheet instance
 * @param {Event} event - The click event
 * @returns {Promise<void>}
 */
export async function onCarryBonus(sheet, event) {
  // Placeholder - will be populated in Phase 4
}
