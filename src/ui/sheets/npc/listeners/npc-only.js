/**
 * NPC-specific listeners (handlers unique to NPC sheets).
 * 
 * Foundry VTT v13 / AppV1-compatible.
 */

/**
 * Register NPC-only event listeners.
 * 
 * @param {npcSheet} sheet - The NPC sheet instance
 * @param {jQuery} html - The rendered sheet HTML
 */
export function registerNpcOnlyListeners(sheet, html) {
  // TODO (Phase 4 continuation):
  // - NPC config toggles
  // - GM-only fields
  // - NPC-specific UI controls
  // - Defend roll (if different from PC)
  // - Any NPC-unique handlers
}
