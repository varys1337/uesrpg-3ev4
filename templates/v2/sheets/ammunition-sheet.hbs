{{!-- Ammunition Sheet - Compact Layout --}}
<header class="item-compact-header">
    <img class="profile-img" src="{{item.img}}" data-edit="img" data-action="editPortrait" title="{{item.name}}" width="64" height="64"/>
    <div class="item-header-fields">
        <input class="item-compact-name" name="name" type="text" value="{{item.name}}" placeholder="{{localize "UESRPG.UI.Name"}}"/>
        <div class="item-stats-row">
            <div class="item-stat">
                <input type="number" name="system.quantity" value="{{item.system.quantity}}" data-dtype="Number"/>
                <span class="item-stat-label">{{localize "UESRPG.Sheets.Item.Quantity"}}</span>
            </div>
            <div class="item-stat">
                <input type="number" name="system.enc" value="{{item.system.enc}}" data-dtype="Number"/>
                <span class="item-stat-label">{{localize "UESRPG.UI.ENC"}}</span>
            </div>
            <div class="item-stat">
                <input type="number" name="system.price" value="{{item.system.price}}" data-dtype="Number"/>
                <span class="item-stat-label">{{localize "UESRPG.Sheets.Equipment.Price"}}</span>
            </div>
            <div class="item-stat">
                <input type="number" name="system.damage" value="{{item.system.damage}}" data-dtype="Number"/>
                <span class="item-stat-label">{{localize "UESRPG.Chat.Common.Damage"}}</span>
            </div>
        </div>
    </div>
</header>

{{!-- Sheet Tab Navigation --}}
<nav class="sheet-tabs tabs" data-group="primary">
    <a class="item" data-action="tab" data-group="primary" data-tab="description">{{localize "UESRPG.UI.Description"}}</a>
    <a class="item" data-action="tab" data-group="primary" data-tab="attributes">{{localize "UESRPG.Sheets.Item.Attributes"}}</a>
    <a class="item" data-action="tab" data-group="primary" data-tab="effects">{{localize "UESRPG.Sheets.Equipment.Effects"}}</a>
</nav>

    {{!-- Sheet Body --}}
    <section class="sheet-body">

        {{!-- Description Tab --}}
        <div class="tab" data-group="primary" data-tab="description">
            <div class="item-description-container">
                <div class="basic-padding">
                    <prose-mirror name="system.description" value="{{item.system.description}}" data-document-uuid="{{item.uuid}}" toggled {{#unless editable}}disabled{{/unless}}>{{{item.system.enrichedDescription}}}</prose-mirror>
                </div>
            </div>
        </div>

        {{!-- Attributes Tab --}}
        <div class="tab" data-group="primary" data-tab="attributes">
            <div class="attribute-tab-container">
                <div class="attribute-container">
                    <div class="item-selection-container">
                        <h2>{{localize "UESRPG.Sheets.Item.Attributes"}}</h2>
                        <div class="selection-container">
                            <div class="grid-container">
                                <div>{{localize "UESRPG.Sheets.Item.DamageType"}}</div>
                                <div class="right-align-content">
                                    <input type="text" name="system.damageType" value="{{item.system.damageType}}" placeholder="{{localize "UESRPG.Sheets.Item.DamageTypePlaceholder"}}"/>
                                </div>
                                <div>{{localize "UESRPG.Sheets.Item.EnchantLevel"}}</div>
                                <div class="right-align-content">
                                    <input type="number" name="system.enchant_level" value="{{item.system.enchant_level}}" data-dtype="Number"/>
                                </div>
                                <div>{{localize "UESRPG.Sheets.Item.Charge"}}</div>
                                <div class="right-align-content enchant-inline enchant-charge-inline">
                                    <button type="button" class="uesrpg-charge-btn" data-action="chargeMinus" title="{{localize "UESRPG.Sheets.Item.DecreaseCurrentCharge"}}">
                                        <i class="fas fa-minus"></i>
                                    </button>
                                    <input type="number" name="system.charge.value" value="{{item.system.charge.value}}" class="enchant-charge-input">
                                    <span class="enchant-charge-sep">/</span>
                                    <input type="number" name="system.charge.max" value="{{item.system.charge.max}}" class="enchant-charge-input">
                                    <button type="button" class="uesrpg-charge-btn" data-action="chargePlus" title="{{localize "UESRPG.Sheets.Item.IncreaseCurrentCharge"}}">
                                        <i class="fas fa-plus"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="quality-container">
                    <div class="flex-container">
                        <h2>{{localize "UESRPG.Sheets.Item.StructuredQualities"}}</h2>
                    </div>

                    <div class="structured-qualities-grid">
                        {{#each coreQualitiesCatalog}}
                            <div class="structured-qualities-cell">
                                <div>{{localize this.label}}</div>
                                <div class="right-align-content">
                                    {{#if this.optionalValue}}
                                        <div class="structured-optional-value">
                                            <input class="checkbox" type="checkbox" name="qualitiesStructured.toggle.{{this.key}}" {{checked (lookup ../qualitiesSelectedToggle this.key)}} />
                                            <input class="structured-value structured-value--opt" type="number" name="qualitiesStructured.value.{{this.key}}" value="{{lookup ../qualitiesSelectedValue this.key}}" min="0" step="1" placeholder="{{localize "UESRPG.Placeholders.NumericValue"}}" />
                                        </div>
                                    {{else}}
                                        {{#if this.hasValue}}
                                            <input class="structured-value" type="number" name="qualitiesStructured.value.{{this.key}}" value="{{lookup ../qualitiesSelectedValue this.key}}" min="{{this.min}}" step="1" />
                                        {{else}}
                                            <input class="checkbox" type="checkbox" name="qualitiesStructured.toggle.{{this.key}}" {{checked (lookup ../qualitiesSelectedToggle this.key)}} />
                                        {{/if}}
                                    {{/if}}
                                </div>
                            </div>
                        {{/each}}
                    </div>


                    <!-- Ammunition does not use an extended trait list; specializations are represented via core qualities. -->
                </div>

                {{#if enchantmentDisplay}}
                <div class="quality-container">
                    <div class="flex-container">
                        <h2>{{localize "UESRPG.Sheets.Item.ActiveEnchantmentRaw"}}</h2>
                    </div>
                    <div class="selection-container">
                        <div class="grid-container">
                            <div>{{localize "UESRPG.Sheets.Equipment.Type"}}</div>
                            <div class="right-align-content">
                                <span class="styled-font-gold">{{enchantmentDisplay.typeLabel}}</span>
                            </div>
                        </div>
                        {{#if enchantmentDisplay.useCharges}}
                        <div class="grid-container">
                            <div>{{localize "UESRPG.Sheets.Item.ChargePool"}}</div>
                            <div class="right-align-content">{{enchantmentDisplay.chargeValue}} / {{enchantmentDisplay.chargeMax}}</div>
                        </div>
                        {{/if}}
                        {{#each enchantmentDisplay.effects as |fx|}}
                        <div class="grid-container">
                            <div>{{fx.label}}</div>
                            <div class="right-align-content u-text-muted">{{fx.paramSummary}}</div>
                        </div>
                        {{/each}}
                        {{#if (eq enchantmentDisplay.typeLabel "Cast")}}
                        <div class="grid-container">
                            <div>{{localize "UESRPG.Sheets.Item.Cast"}}</div>
                            <div class="right-align-content">
                                <button type="button" data-action="castEnchantment">{{localize "UESRPG.Sheets.Equipment.CastEnchantment"}}</button>
                            </div>
                        </div>
                        {{/if}}
                    </div>
                </div>
                {{/if}}

                {{#if uiSpellcastingConfig}}
                {{> "systems/uesrpg-3ev4/templates/partials/sheets/item-spellcasting-config.hbs" helpKey="UESRPG.Sheets.Item.SpellcastingConfigurationAmmoHelp" uiSpellcastingConfig=uiSpellcastingConfig}}
                {{/if}}
            </div>
        </div>

        {{!-- Effects Tab --}}
        <div class="tab effects" data-group="primary" data-tab="effects">
            {{> "systems/uesrpg-3ev4/templates/partials/sheets/effects-tab.hbs"}}
        </div>
    </section>
