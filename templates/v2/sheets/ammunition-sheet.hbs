{{!-- Ammunition Sheet вЂ” Compact Layout --}}
<header class="item-compact-header">
    <img class="profile-img" src="{{item.img}}" data-edit="img" data-action="editPortrait" title="{{item.name}}" width="64" height="64"/>
    <div class="item-header-fields">
        <input class="item-compact-name" name="name" type="text" value="{{item.name}}" placeholder="Name"/>
        <div class="item-stats-row">
            <div class="item-stat">
                <input type="number" name="system.quantity" value="{{item.system.quantity}}" data-dtype="Number"/>
                <span class="item-stat-label">Quantity</span>
            </div>
            <div class="item-stat">
                {{#if item.system.encEffective}}
                    <input type="text" value="{{item.system.encEffective}}" readonly/>
                    <input type="number" name="system.enc" value="{{item.system.enc}}" data-dtype="Number" class="u-hidden"/>
                {{else}}
                    <input type="number" name="system.enc" value="{{item.system.enc}}" data-dtype="Number"/>
                {{/if}}
                <span class="item-stat-label">ENC</span>
            </div>
            <div class="item-stat">
                {{#if item.system.priceEffective}}
                    {{#unless item.system.gmOverride.useBaseStats}}
                        <input type="text" value="{{item.system.priceEffective}}" readonly/>
                        <input type="number" name="system.price" value="{{item.system.price}}" data-dtype="Number" class="u-hidden"/>
                    {{else}}
                        <input type="number" name="system.price" value="{{item.system.price}}" data-dtype="Number"/>
                    {{/unless}}
                {{else}}
                    <input type="number" name="system.price" value="{{item.system.price}}" data-dtype="Number"/>
                {{/if}}
                <span class="item-stat-label">Price</span>
            </div>
            <div class="item-stat">
                {{#if item.system.damageEffective}}
                    {{#unless item.system.gmOverride.useBaseStats}}
                        <input type="text" value="{{item.system.damageEffective}}" readonly/>
                        <input type="number" name="system.damage" value="{{item.system.damage}}" data-dtype="Number" class="u-hidden"/>
                    {{else}}
                        <input type="number" name="system.damage" value="{{item.system.damage}}" data-dtype="Number"/>
                    {{/unless}}
                {{else}}
                    <input type="number" name="system.damage" value="{{item.system.damage}}" data-dtype="Number"/>
                {{/if}}
                <span class="item-stat-label">Damage</span>
            </div>
            <div class="item-stat">
                <input type="number" name="system.enchant_level" value="{{item.system.enchant_level}}" data-dtype="Number"/>
                {{#if item.system.enchant_levelEffective}}
                    <div class="effective-hint"><span class="label">Eff</span>{{item.system.enchant_levelEffective}}</div>
                {{/if}}
                <span class="item-stat-label">Enchant Lv.</span>
            </div>
        </div>
    </div>
</header>

{{!-- Sheet Tab Navigation --}}
<nav class="sheet-tabs tabs" data-group="primary">
    <a class="item" data-action="tab" data-group="primary" data-tab="description">Description</a>
    <a class="item" data-action="tab" data-group="primary" data-tab="attributes">Attributes</a>
    <a class="item" data-action="tab" data-group="primary" data-tab="effects">Effects</a>
</nav>

    {{!-- Sheet Body --}}
    <section class="sheet-body">

        {{!-- Description Tab --}}
        <div class="tab" data-group="primary" data-tab="description">
            <div class="item-description-container">
                <div class="basic-padding">
                    {{editor item.system.description target="system.description" engine="prosemirror" button=true owner=owner editable=editable}}
                </div>
            </div>
        </div>

        {{!-- Attributes Tab --}}
        <div class="tab" data-group="primary" data-tab="attributes">
            <div class="attribute-tab-container">
                <div class="attribute-container">
                    <div class="item-selection-container">
                        <h2>Attributes</h2>
                        <div class="selection-container">
                            <div class="grid-container">
                                <div>Price / 10</div>
                                <div class="right-align-content">
                                    <input type="number" name="system.pricePer10" value="{{item.system.pricePer10}}" data-dtype="Number"/>
                                    {{#if item.system.pricePer10Effective}}
                                        <div class="effective-hint"><span class="label">Eff</span>{{item.system.pricePer10Effective}}</div>
                                    {{/if}}
                                </div>

                                <div>Price / Shot</div>
                                <div class="right-align-content">
                                    <input type="text" value="{{item.system.pricePerShot}}" data-uesrpg="pricePerShot" readonly/>
                                    {{#if item.system.pricePerShotEffective}}
                                        <div class="effective-hint"><span class="label">Eff</span>{{item.system.pricePerShotEffective}}</div>
                                    {{/if}}
                                </div>
<div>Ammo Material</div>
                                <div class="right-align-content">
                                    <select name="system.ammoMaterial">
                                        {{selectOptions ammoMaterialOptions selected=item.system.ammoMaterial}}
                                    </select>
                                </div>
                                <div>Manual Base Stats</div>
                                <div class="right-align-content">
                                    <input class="checkbox" type="checkbox" name="system.gmOverride.useBaseStats" {{checked item.system.gmOverride.useBaseStats}}>
                                </div>

                                <div>Ignore ENC</div>
                                <div class="right-align-content">
                                    <input class="checkbox" type="checkbox" name="system.excludeENC" {{checked item.system.excludeENC}}>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="quality-container">
                    <div class="flex-container">
                        <h2>Structured Qualities</h2>
                    </div>

                    <div class="structured-qualities-grid">
                        {{#each coreQualitiesCatalog}}
                            <div class="structured-qualities-cell">
                                <div>{{this.label}}</div>
                                <div class="right-align-content">
                                    {{#if this.optionalValue}}
                                        <div class="structured-optional-value">
                                            <input class="checkbox" type="checkbox" name="qualitiesStructured.toggle.{{this.key}}" {{checked (lookup ../qualitiesSelectedToggle this.key)}} />
                                            <input class="structured-value structured-value--opt" type="number" name="qualitiesStructured.value.{{this.key}}" value="{{lookup ../qualitiesSelectedValue this.key}}" min="0" step="1" placeholder="X" />
                                        </div>
                                    {{else}}
                                        {{#if this.hasValue}}
                                            <input class="structured-value" type="number" name="qualitiesStructured.value.{{this.key}}" value="{{lookup ../qualitiesSelectedValue this.key}}" min="{{this.min}}" step="1" />
                                        {{else}}
                                            <input class="checkbox" type="checkbox" name="qualitiesStructured.toggle.{{this.key}}" {{checked (lookup ../qualitiesSelectedToggle this.key)}} />
                                        {{/if}}
                                    {{/if}}
                                </div>
                            </div>
                        {{/each}}
                    </div>


                    <!-- Ammunition does not use an extended trait list; specializations are represented via core qualities. -->
                </div>
            </div>
        </div>

        {{!-- Effects Tab --}}
        <div class="tab effects" data-group="primary" data-tab="effects">
            {{> "systems/uesrpg-3ev4/templates/partials/sheets/effects-tab.hbs"}}
        </div>
    </section>

